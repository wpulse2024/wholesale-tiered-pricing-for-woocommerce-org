/**
 * Wholesale Report Page - optional JS enhancements
 * (Report data is loaded server-side; this handles any future interactive features)
 */
(function ($) {
    'use strict';

    $(function () {
        // Report filter form could be enhanced for AJAX reload here if needed
        // Currently uses standard form submit for date filtering
    });
})(jQuery);
